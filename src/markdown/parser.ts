// custom markdown parser
// using markdown it https://www.markdownguide.org/extended-syntax
// custom heading id https://www.markdownguide.org/extended-syntax#heading-ids
import path from "path";
const rootPath = path.join(__dirname, "/../../");
const srcPost = path.join(rootPath, "src-posts");
console.log(rootPath, srcPost);
